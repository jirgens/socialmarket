.row
  %br
  .well
    %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(@user.email.downcase)}?s=50")
    %strong= @user.username
    %p User since #{@user.created_at.to_formatted_s(:long)}
    %br
    %p add blerb column for bio blerb
    = link_to "Follow", relationships_path(leader_id: @user.id), class: "btn"
    %br
    .row
      - @user.recent_products.each do |product|
        = link_to product do
          = image_tag product.pic1_url.to_s
    .row
      = link_to '' do
        %img(src="http://lorempixel.com/150/150")
